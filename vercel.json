{
  "buildCommand": "cd front && npm install && CI=false npm run build",
  "outputDirectory": "front/build",
  "framework": "create-react-app",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ]
}
